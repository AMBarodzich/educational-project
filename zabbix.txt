1. нужно два сервака
2. ssh между двумя серваками
3. нужны порты открытые 10050 - клиентская часть и 10051 серверная
4. На сервере установить
https://www.zabbix.com/ru/download?zabbix=4.0&os_distribution=ubuntu&os_version=20.04_focal&db=mysql&ws=apache

5. agent
wget https://repo.zabbix.com/zabbix/4.0/ubuntu/pool/main/z/zabbix-release/zabbix-release_4.0-3+focal_all.deb
dpkg -i zabbix-release_4.0-3+focal_all.deb
sudo apt-get update
sudo apt install zabbix-agent -y

cp zabbix_agentd.conf /etc/zabbix/
в конфиге поменять Server на тот где заббикс
Server=3.19.54.78
ServerActive=3.19.54.78
Hostname=agent-1


sudo apt install nginx -y
sudo systemctl enable nginx
sudo systemctl start nginx

6. Configuration -> hosts
7. create host
8. в первой вкладке - виртуал машинс, второй в вкладке темплейтах добавить template OS Linux, Template App HTTP, Template ssh

































fix database
SET GLOBAL innodb_strict_mode = 0;
нет порта 10051 поставить DBPassword=password